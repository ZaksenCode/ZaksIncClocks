package com.zaksen.zaksincclocks.utils;

import com.zaksen.zaksincclocks.data.LocationArray;
import org.bukkit.Location;

import java.util.Collections;
import java.util.List;

public class TextToLocation {

    public static LocationArray charToLocation(Location offset, char chr) {
        // TODO - преобразование символа в массив позиций

        return new LocationArray();
    }

    public static List<LocationArray> textToLocations(Location offset, String text) {
        // TODO - преобразование символа в массив из массивов позиций

        return Collections.singletonList(new LocationArray());
    }
}
